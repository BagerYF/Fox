enum LoadingType {
  none,
  transparent,
  display,
}

enum ExceptionType {
  network,
  server,
  back,
  retry,
  toast,
}

enum PageStatus {
  init,
  success,
  fail,
  normal,
  processing,
  error,
  loading,
  empty,
  webview,
}
